<div class="homepage">
  <div class='form-upload'>
  </div>

  <div class="posts">
    <% @posts.each do |post| %>
      <%= render partial: 'post', locals: { post: post } %>
    <% end %>
  </div>
</div>
